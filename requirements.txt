pandas>=2.0
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
openpyxl>=3.1
xlrd>=2.0
PyYAML>=6.0

# Only needed to run the dashboards in enrollment/apps/ locally - not used
# by the scraper/clean/compile scripts or the GitHub Actions workflows.
streamlit>=1.30
plotly>=5.20
